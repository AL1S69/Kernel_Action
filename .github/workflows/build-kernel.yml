name: Build Kernel

on:
  workflow_dispatch:
    inputs:
      KERNEL_SOURCE_BRANCH:
        description: 'Kernel source branch to build'
        required: true
        default: 'lineage-21.0'

jobs:
  build:
    name: Build Kernel
    runs-on: ubuntu-22.04
    env:
      ARCH: arm64
      KERNEL_IMAGE_NAME: Image.gz-dtb
      KERNEL_IMAGE_NAME_MKBOOTIMG: Image.gz
      LOCALVERSION: -custom
      UPLOADNAME: ''
      DEVICE: lavender
      BUILD_TIME: ${{ github.run_id }}
      FORMAT_MKBOOTING: "--kernel out/kernel --ramdisk out/ramdisk.img --base 0x00000000 --pagesize 4096"
      HAVE_SOURCE_BOOT_IMAGE: true
      NEED_DTBO: true
      CHECK_FILE_IS_OK: false
      CHECK_DTBO_IS_OK: false
      MAKE_BOOT_IMAGE_IS_OK: false

    steps:
    - name: Checkout Kernel Source
      uses: actions/checkout@v4

    - name: Setup dependencies
      run: |
        sudo apt update
        sudo apt install -y git curl wget python3 unzip bc bison flex libssl-dev build-essential \
          libncurses5-dev libelf-dev python3-pip
        pip3 install pycryptodome

    - name: Build Kernel
      run: |
        cd kernel_workspace
        # 示例构建命令（你可替换为具体的构建脚本）
        make O=out ARCH=${{ env.ARCH }} ${KERNEL_DEFCONFIG}
        make -j$(nproc) O=out ARCH=${{ env.ARCH }}

        # 检查是否成功输出内核镜像
        if [ -f android-kernel/out/arch/${{ env.ARCH }}/boot/${{ env.KERNEL_IMAGE_NAME }} ]; then
            echo "CHECK_FILE_IS_OK=true" >> $GITHUB_ENV
        else
            echo "Kernel image not found"
            exit 1
        fi

    - name: Check DTBO image
      if: ${{ env.NEED_DTBO == 'true' }}
      run: |
        if [ -f android-kernel/out/arch/${{ env.ARCH }}/boot/dtbo.img ]; then
            echo "CHECK_DTBO_IS_OK=true" >> $GITHUB_ENV
        else
            echo "DTBO image is empty"
            exit 1
        fi

    - name: Make Anykernel3
      if: ${{ env.CHECK_FILE_IS_OK == 'true' }}
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace
        git clone https://github.com/osm0sis/AnyKernel3
        sed -i 's/do.devicecheck=1/do.devicecheck=0/g' AnyKernel3/anykernel.sh
        sed -i 's!block=/dev/block/platform/omap/omap_hsmmc.0/by-name/boot;!block=auto;!g' AnyKernel3/anykernel.sh
        sed -i 's/is_slot_device=0;/is_slot_device=auto;/g' AnyKernel3/anykernel.sh
        cp android-kernel/out/arch/${{ env.ARCH }}/boot/${{ env.KERNEL_IMAGE_NAME }} AnyKernel3/
        if [ ${{ env.CHECK_DTBO_IS_OK }} = true ]; then
            cp android-kernel/out/arch/${{ env.ARCH }}/boot/dtbo.img AnyKernel3/
        fi
        rm -rf AnyKernel3/.git* AnyKernel3/README.md

    - name: Make boot image
      if: ${{ env.HAVE_SOURCE_BOOT_IMAGE == 'true' && env.CHECK_FILE_IS_OK == 'true' }}
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace
        tools/unpack_bootimg.py --boot_img boot-source.img
        cp android-kernel/out/arch/${{ env.ARCH }}/boot/${{ env.KERNEL_IMAGE_NAME_MKBOOTIMG }} out/kernel
        tools/mkbootimg.py ${{ env.FORMAT_MKBOOTING }} -o boot.img
        if [ -f boot.img ]; then
            echo "MAKE_BOOT_IMAGE_IS_OK=true" >> $GITHUB_ENV
        else
            echo "Boot image is empty"
            exit 1
        fi

    - name: Upload Kernel Image
      if: ${{ env.CHECK_FILE_IS_OK == 'true' }}
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.KERNEL_IMAGE_NAME }}${{ env.LOCALVERSION }}${{ env.UPLOADNAME }}-${{ env.DEVICE }}-${{ env.BUILD_TIME }}
        path: kernel_workspace/android-kernel/out/arch/${{ env.ARCH }}/boot/${{ env.KERNEL_IMAGE_NAME }}

    - name: Upload AnyKernel3
      if: ${{ env.CHECK_FILE_IS_OK == 'true' }}
      uses: actions/upload-artifact@v4
      with:
        name: AnyKernel3${{ env.LOCALVERSION }}${{ env.UPLOADNAME }}-${{ env.DEVICE }}-${{ env.BUILD_TIME }}
        path: kernel_workspace/AnyKernel3/*

    - name: Upload DTBO image
      if: ${{ env.CHECK_DTBO_IS_OK == 'true' }}
      uses: actions/upload-artifact@v4
      with:
        name: dtbo-${{ env.DEVICE }}-${{ env.BUILD_TIME }}
        path: kernel_workspace/android-kernel/out/arch/${{ env.ARCH }}/boot/dtbo.img

    - name: Upload boot image
      if: ${{ env.MAKE_BOOT_IMAGE_IS_OK == 'true' }}
      uses: actions/upload-artifact@v4
      with:
        name: boot${{ env.LOCALVERSION }}${{ env.UPLOADNAME }}-${{ env.DEVICE }}-${{ env.BUILD_TIME }}
        path: kernel_workspace/boot.img
